### 지역 설정 간소화

- 전국 모든 '구(區)' 제거 → 시 단위로 통합 (예: 수원시 장안구·팔달구 → 수원시)
- 광역시(서울·부산·대구·인천·광주·대전·울산)도 구 없이 시 단위로만

### 제목 순서 고정

- 제목이 항상 **주요키워드(지역+업종) → 업체명 → 수식어** 순서로 나오도록 고정
- 수식어가 업체명보다 앞서 나오던 문제 해결

### 생성된 포스트 보기 — 날짜별 탭

- 생성된 포스트를 생성일(날짜)별로 묶어 보는 '날짜별' 탭 추가

### 로그인 안정화

- 지역 크롤링 로그인이 옛 버튼(log.login)을 기다리다 실패하던 문제 수정 (신 loginBtn_row/column 대응)
- 세션 없는 2명의 아이디가 로그인창에서 멈추던 증상 해결
