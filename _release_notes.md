## v2.5.16 변경사항

### 포스트 글쓰기가 안 눌리던 진짜 원인 수정 (핵심)
- 크롤 후 '포스트 글쓰기'를 누르면 업체선택 목록을 만드는 과정에서 내부 오류(튜플 언패킹)로 조용히 실패하던 문제 수정. 이제 정상적으로 업체선택 창이 뜹니다. (F8 발행 목록도 동일 수정)
- 오류가 나도 아무것도 안 뜨게 만들던 로그 기록 버그(datetime 충돌)도 수정 → 앞으로 문제가 생기면 반드시 표시/기록됩니다.

## v2.5.15 변경사항

### 불필요한 서버(Firebase) 읽기·쓰기 추가 제거 (할당량 절약)
- 세션 하트비트 제거: 다중 로그인 허용이라 세션 값을 아무 데서도 안 쓰는데 5분마다 서버 쓰기를 하던 것 삭제.
- 로그인/종료 시 세션 등록·삭제 쓰기도 제거(같은 이유).
- 회원가입 중복 확인을 '전체 회원 읽기' → '해당 아이디 1개만' 확인으로 변경.
- (v2.5.13의 로그인·이용기간 확인 단일읽기+캐시와 합쳐, 서버 사용량을 최소화)

## v2.5.14 변경사항

### 데이터를 '총폴더' 하나로 정리 (크롤/글/이미지/기타)
- 실행 폴더에 **블로그마스터_데이터** 폴더 하나를 만들어 그 안에 정리 저장:
  - **크롤한목록/** — 수집물(crawled) + 크롤 로그(logs)
  - **작성한글/** — 생성된 포스트(generated_posts)
  - **이미지/** — 포스트 이미지
  - **기타/** — 예약상태·발행일정·검색기록·캐시 등
- 기존에 흩어져 있던 데이터(_internal·실행폴더)는 첫 실행 시 **총폴더로 자동 정리 이동**(유실 없음). 아직 이동 안 된 옛 위치도 읽기 폴백으로 그대로 인식.

## v2.5.13 변경사항

### 서버(Firebase) 읽기 폭발로 로그인·인증이 막히던 문제 수정 (중요)
- 로그인·글쓰기·크롤 버튼마다 '전체 회원 명단'을 통째로 읽어 무료 할당량을 소진하던 문제 수정.
- 로그인·이용기간 확인을 '본인 1명만 읽기'로, 반복 조회는 60초 캐시로 처리 → 서버 읽기량 대폭 감소(할당량 초과 재발 방지).
- 세션 하트비트도 불필요한 읽기 제거.

### 글쓰기 진단 로그 추가
- '포스트 글쓰기'가 안 눌릴 때 어느 단계에서 막혔는지 crash_log.txt에 기록 → 원인 즉시 확인 가능.

## v2.5.12 변경사항

### 글쓰기·크롤이 "안 눌리던" 진짜 원인 수정 (중요)
- 로그인 계정의 개인 API 키가 내부(api_keys_by_user)에만 남고 글쓰기·크롤이 읽는 위치로 반영되지 않아, 키가 있는데도 "API 키 필요"로 막혀 글쓰기·"포스팅할 업체 선택" 목록이 뜨지 않던 문제 수정.
- 이제 로그인한 계정의 키를 자동으로 반영해 글쓰기·크롤·발행이 정상 동작.
- 데이터가 아직 _internal 에 있어 자동이동이 안 된 경우에도 목록을 그대로 읽도록 보완(유실 방지).

## v2.5.11 변경사항

### 크롤 목록 안 나오던 문제 + 저장 위치 정리 (중요)
- 크롤 목록이 안 뜨던 원인 수정: 데이터는 계정키로 저장하는데 조회 시 blog_id(_acc0)를 덧붙여 엉뚱한 빈 폴더를 보던 문제.
- **저장 위치를 BlogMaster.exe 실행 폴더로 변경** — 수집·생성글·이미지 등 모든 데이터가 exe 옆(실행 폴더)에 저장됨. (예전엔 _internal 안이라 헷갈리고 재설치에 취약)
- 기존 _internal 안 데이터는 첫 실행 시 exe 폴더로 **자동 이동**(데이터 그대로 보존).

## v2.5.10 변경사항

### 글쓰기 안 되던 문제 긴급 수정 (핫픽스)
- v2.5.8~2.5.9에서 글쓰기(포스트 생성)가 내부 오류로 조용히 실패하던 문제 수정.
- API 폴백(제미나이→딥시크→챗GPT) 정상 동작.

## v2.5.9 변경사항

### 재설치 시 작업물(데이터) 보존 (중요)
- 그동안 자동업데이트가 실패해 설치파일로 다시 설치하면 _internal 폴더를 통째로 지워 생성글·크롤·이미지가 사라지던 문제 수정.
- 이제 재설치해도 덮어쓰기만 하고 사용자 데이터(생성글/크롤결과/이미지/로그)는 그대로 보존.
- (자동업데이트 zip 방식은 원래부터 데이터 보존됨 — 이번 수정은 '설치파일 재설치' 경로용.)

## v2.5.8 변경사항

### 글쓰기 API 자동 폴백 (제미나이 → 딥시크 → 챗GPT)
- 글쓰기를 **제미나이로 먼저** 시도하고, 실패(에러/한도)하면 **딥시크**, 그것도 안 되면 **챗GPT**로 자동 전환.
- 키가 있는 API만 순서대로 사용. 포스트별로 실제 사용된 API가 목록의 API 컬럼에 기록됨.

### 목록에 '기타' 안 뜨게
- 타임스탬프 없는 옛 크롤 파일은 파일 수정시각으로 날짜를 대체 → 정체불명 '📅 기타' 그룹 제거.

## v2.5.7 변경사항

### 목록 그룹 = '크롤한 키워드' 하나당 드롭다운 하나
- 이전 버전의 '30분 이내 묶음'(임의 시간창) 제거.
- 이제 (크롤 시각 + 검색어) 단위로 묶어, 키워드 한 번 크롤 = 드롭다운 하나 (예: 🕐 14:44 · 파산신청).
- 서로 다른 키워드(파산신청·변호사)는 각각 별도 드롭다운. 같은 키워드를 다른 시각에 두 번 크롤해도 각각 분리. F7·F8 동일.

## v2.5.6 변경사항

### 업종별 맞춤 프롬프트 추가
- 요양병원, 암요양병원 업종 전용 글쓰기 프롬프트 추가.

## v2.5.5 변경사항

### 글쓰기 시 사용 API 표시
- 포스트 생성(글쓰기) 시작할 때 로그에 '글쓰기 API: 딥시크/챗GPT/제미나이' 표시.
- 상단 상태바에도 '포스트 생성 중... (딥시크)'처럼 현재 사용 엔진 표시.

## v2.5.4 변경사항

### 목록 세션 라벨에 검색어 표시
- 시간 그룹(세션)에 시각뿐 아니라 **검색어(업종)**도 함께 표시 (예: 🕐 11:43 · 요양원).
- 한 세션에서 여러 검색어를 크롤한 경우 함께 나열 (예: 요양원, 헬스장 …). F7·F8 동일.

## v2.5.3 변경사항

### 자동댓글 속도 개선
- 글마다 고정으로 걸리던 대기(이동 2.5초 + 정착 2.0초)를 '댓글 뜨면 즉시 진행'하는 방식으로 변경.
- 답글창 열림·제출 대기시간 단축, 댓글 목록 페이징도 불필요한 고정 대기 제거.
- 전체 자동관리(서이추+자동댓글)가 눈에 띄게 빨라짐.

### 댓글·이웃 관리 로그창 확대
- 하단 로그창을 위아래로 크게(90 → 240px) 늘려 진행 상황이 더 많이 보이게.

## v2.5.2 변경사항

### 목록 시간 그룹 = '한 번의 크롤' 단위로
- 시간 그룹을 분(minute) 단위가 아니라 **한 번에 크롤한 것끼리** 묶음 (F7·F8 동일).
- 크롤 시 세션의 모든 키워드에 같은 시각을 부여 → 새로 크롤하는 건 항상 한 묶음.
- 기존 데이터도 가까운 시각(30분 이내)끼리 자동으로 한 세션으로 묶어 표시.

## v2.5.1 변경사항

### 포스트 글쓰기 목록도 4단 통일
- '포스팅할 업체 선택'(F7) 목록을 생성된 포스트 보기(F8)와 똑같이 **📅 날짜 → 🕐 시간 → 지역·업종 → 업체** 4단으로 통일.
- 전체선택·2차필터·선택삭제·완료제거 등 모든 동작이 4단에서 정상 작동하도록 정리.

## v2.5.0 변경사항

### 예약발행 간격 오류 수정 (중요)
- 기존 예약을 모두 확인해서, 새 글이 어떤 예약과도 '설정 간격 − 20분'보다 가깝게 붙지 않도록 배치.
- 기존 예약이 촘촘하면 그 뒤로 간격대로 이어가고, 큰 빈 구간은 간격대로 채움 → 2시간 ±20분처럼 설정한 간격이 항상 지켜짐 (예전엔 50~70분 간격으로 끼어드는 문제).
- 예약 목록을 스크롤하며 전부 읽어옴 (가상 스크롤이라 일부만 읽히던 문제 보완).

### 생성된 포스트 보기 — 날짜 밑에 시간 단위 추가
- 목록을 📅 날짜 → 🕐 시간 → 지역·업종 → 업체 4단으로 세분화. 같은 시각 수집분이 한 묶음으로.

## v2.4.9 변경사항

### 지역 설정 — 구(區) 복구
- 앞서 제거했던 구를 다시 살림. 각 시 밑에 그 시의 구들이 묶여 나옴 (수원시 → 장안구·권선구·팔달구·영통구 등).

### 생성된 포스트 보기 — '포스트 글쓰기'와 동일한 3단 목록
- 목록을 F7 '포스팅할 업체 선택'과 똑같이 **📅 날짜 → 지역·업종 그룹 → 업체** 3단으로 통일 (별도 '날짜별' 탭 제거).
- 같은 날 수집분이 한 날짜 아래로 묶임. 포스트별 사용 API(딥시크/챗GPT/제미나이) 컬럼 표시.

## v2.4.8 변경사항

### 생성된 포스트 보기 — 날짜·시간 배치 + API 표시
- '날짜별' 탭을 생성 배치(날짜+시간) 단위로 묶음 — 같은 시각에 생성한 글이 한 묶음으로.
- 각 배치에 사용한 글쓰기 API(딥시크/챗GPT/제미나이) 표시 + 포스트별 'API' 컬럼 추가.
- (기존 글은 생성시각·API 정보가 없어 '날짜 미상'으로 표시되며, 이후 생성분부터 정상 기록.)

### 키워드마스터 연동 제거
- 시작 시 키워드마스터 핸드오프 파일을 읽어오던 수신 기능 제거 (양방향 연동 종료).

## v2.4.7 변경사항

### 지역 설정 간소화
- 전국 모든 '구(區)' 제거 — 시 단위로 통합 (예: 수원시 장안구·팔달구 → 수원시).
- 광역시(서울·부산·대구·인천·광주·대전·울산)도 구 없이 시 단위로만.

### 제목 순서 고정
- 제목이 항상 '주요키워드(지역+업종) → 업체명 → 수식어' 순서로 나오도록 고정 (수식어가 업체명보다 앞서던 문제 해결).

### 생성된 포스트 보기 — 날짜별 탭
- 생성된 포스트를 생성일(날짜)별로 묶어 보는 '날짜별' 탭 추가.

### 로그인 안정화
- 지역 크롤링 로그인이 옛 버튼 셀렉터(log.login)를 기다리다 실패하던 문제 수정 (신 loginBtn_row/column 대응). 세션 없는 2명의 아이디가 로그인창에서 멈추던 증상 해결.

## v2.4.6 변경사항

### 신규: 전체 자동 관리
- 댓글·이웃 관리 창에 '🔁 전체 자동 관리' 버튼 추가.
- 결제된 모든 아이디를 순서대로 자동 로그인하며 아이디마다 ① 서이추 자동 수락 ② 삭제요청 의심 댓글의 원글 자동 삭제 ③ 공감·자동답글을 일괄 처리.
- 미결제/만료 아이디는 자동 건너뜀, '중단' 버튼으로 언제든 정지. 자동 삭제 전 1회 확인창.

### 자동답글 문구 확장
- 자동답글 문구 풀 500 → 2000개로 확장 (봇 느낌 최소화, 중복 없이 다양화).

## v2.4.5 변경사항

### 예약발행 밀림 수정
- 예약 기준 시각(base_time)을 기존 예약 맨 뒤로 밀지 않고 '지금'부터 유지.
- 이미 예약된 10분 슬롯과 이번 배치 슬롯만 건너뛰어 빈 시간에 채움.
- 멀리 박힌 예약 하나 때문에 전체 일정이 며칠~몇 주씩 밀리던 문제 해결.

## v2.4.4 변경사항

### 신규: 발행 일정표(날짜별)
- 발행/예약 시 날짜·주제(키워드)·명의·상태를 `posting_schedule_{계정}.json`에 자동 기록.
- 대시보드 '발행일정표' 버튼 → 날짜별 '무슨 주제 몇 개' 요약 + 명의 필터.
- 기존 발행 로직 무변경 (성공 시 기록 한 줄만 추가, 실패해도 무시).

## v2.4.3 변경사항

### 네이버 로그인 복구 + 사진먼저 배치
- 네이버 로그인 페이지 개편으로 깨진 자동로그인 수정 (`log.login` → `loginBtn_row/column`).
- ID/PW 입력을 정확값 설정(JS)으로 안정화 (오토필/타이핑 오류 방지).
- `_normalize_image_markers`: `image_layout=top`(사진 먼저 상단 일괄) 기본, `spread`(교차) 전환 가능.
- 키워드마스터 연동 수신(v2.4.2) 포함.

## v2.4.2 변경사항

### 신규: 키워드마스터 연동 (핸드오프 수신)
- 시작 시 `%LOCALAPPDATA%\BlogMaster\selected_keywords.json` 자동 수신 → 키워드 입력 목록/히스토리에 반영 (파일 소비, 중복 방지).
- 순수 추가(79줄), 기존 크롤·글쓰기·발행·계정 기능 변경 없음.

## v2.4.1 변경사항

### 자동 업데이트 안정화
- 같은 버전 자동패치가 실패하면 재프롬프트 루프를 멈추고 수동설치 1회 안내 (`config update_attempted_version`).
- robocopy 복사 실패 시 거짓 OK 대신 `ROBOCOPY_FAIL` 기록 후 구버전 안전 재실행.
- 설치폴더 쓰기 불가 시 UAC 승격으로 복사.
- 신규 모듈 `comment_buddy_manager`, `local_image_store` 추적 추가.

## v1.9.3 변경사항

### 신규: 이웃·댓글 관리
- 명의별 네이버 계정으로 로그인해 서로이웃 신청 수락/거절을 일괄 처리.
- 받은 댓글 목록을 불러와 삭제 요청 댓글 확인, 게시글 선택 삭제 가능.
- 댓글 공감(engage) 자동 처리 및 답글 문구 적용.
- 명의/아이디 전환 시 이전 크롬 세션 정리 후 재로그인.

### 안정화
- 현재까지 반영된 글쓰기·이미지·지역·결제 로직 그대로 빌드.
